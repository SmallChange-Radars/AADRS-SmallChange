import { ClientPortfolio } from './client-portfolio';

describe('ClientPortfolio', () => {
  it('should create an instance', () => {
    expect(new ClientPortfolio('',-1,-1,-1,-1,-1)).toBeTruthy();
  });
});
